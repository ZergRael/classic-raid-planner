import { MAX_PLAYERS } from "@/constants/global";

const state = () => ({
  playerCount: MAX_PLAYERS,
});

const getters = {};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
