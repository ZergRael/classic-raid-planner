const state = () => ({
  drag: false,
});

const getters = {};

const actions = {};

const mutations = {
  setDrag(state, drag) {
    state.drag = drag;
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
